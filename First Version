const updateBlock = (value) => {
};


const numbers = [

]

const sumArrayOfNumbers =(numbersArray) => {
   let total = 0;

   for (
     let i = 0;
     i < numbersArray.length;
     i += 1
   ) {
     total += numbersArray[i];
   }

   return total;


}


updateBlock(sumArrayOfNumbers(numbers));
